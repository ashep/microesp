[tox]
envlist =
    py3{5,6,7}

[testenv]
passenv = LANG
deps =
    pytest
    coverage
    adafruit-ampy==1.*

commands =
    coverage erase
    coverage run --source microesp -m pytest -x -s tests
    coverage html
